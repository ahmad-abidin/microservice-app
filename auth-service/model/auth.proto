syntax = "proto3";

package model;

message Identity {
    string username = 1;
    string password = 2;
}

message Credential {
    string token = 1;
}

service Auth {
    rpc Authentication(Identity) returns (Credential) {}
}