syntax = "proto3";

option go_package = "github.com/ahmad-abidin/microservice-app/auth-service";

package auth;

message Identity {
    string username = 1;
    string password = 2;
}

message Credential {
    string token = 1;
}

service Auth {
    rpc Authentication(Identity) returns (Credential) {}
}